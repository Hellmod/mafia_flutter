import 'package:flutter/material.dart';

class LobbyTokenScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Lobby Tocken Screen')),
      body: Text('Lobby Tocken Screen'),
    );
  }
}
